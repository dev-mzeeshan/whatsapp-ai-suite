# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Settings & validation
pydantic==2.9.2
pydantic-settings==2.5.2

# Async HTTP client (Meta API calls)
httpx==0.27.2


pwdlib

# Database (async)
sqlalchemy==2.0.35
asyncpg==0.29.0
alembic==1.13.3

# Redis (rate limiting + context cache — Phase 2)
redis[asyncio]==5.1.1

# Security
python-jose[cryptography]==3.3.0   # JWT (Phase 3 auth)
passlib[bcrypt]==1.7.4

# Dev / testing
pytest==8.3.3
pytest-asyncio==0.24.0
httpx                               # already pinned above, used in test client
ruff==0.6.9                         # linter + formatter
mypy==1.11.2                        # type checker